module.exports = function getFile (at) {
  // TODO
  // if (typeof window !== 'undefined') {
  //   // XHR
  // } else {
  //   if (isURL(at)) {
  //     request(at);
  //   } else {
  //     fs.readFileSync(at);
  //   }
  // }
  return at;
};
